/**
 * Exercice 1.1
 * Créer une variable avec du texte, deux autres avec des nombres.
 * Afficher à l’écran, celle avec du texte, et dans la console, la soustraction des deux variables numériques
*/

// On déclare une variable text à laquelle on affecte la valeur "test"
let text = 'test';

// On déclare une variable nombre1 à laquelle on affecte la valeur 1
let nombre1 = 2;

// On déclare une variable nombre2 à laquelle on affecte la valeur 2
let nombre2 = 1;

// On affiche les deux nombres et leur soustraction dans la console
console.log('nombre1 - nombre2 =', nombre1 - nombre2);

// On affiche le texte à l'écran avec la méthode alert (à la fin pour ne pas interrompre le script)
alert(text);
